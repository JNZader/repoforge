"""Provider key management routes — CRUD for LLM API keys.

Placeholder: endpoints will be implemented in Phase 3.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/providers", tags=["providers"])
