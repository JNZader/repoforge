"""Generation routes — start, stream, cancel, download.

Placeholder: endpoints will be implemented in Phase 4.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/generate", tags=["generate"])
