"""History routes — list, detail, delete generations.

Placeholder: endpoints will be implemented in Phase 5.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/history", tags=["history"])
