"""Analytics routes — usage stats, model breakdown, top repos.

Placeholder: endpoints will be implemented in Phase 5.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/analytics", tags=["analytics"])
